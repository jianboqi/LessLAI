{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python Debugger: Current File with Arguments",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "args": [
                "-i", "Landsat_0618(3bands).tif",   // 注意：两个独立字符串
                "-o", "lai_0618_ref1.tif",
                "-sza", "25"
                //,"--cpu"          // 需要时取消注释即可
            ]
        }
    ]
}